import { default as markdownit } from 'markdown-it/lib';

export interface CodeTabsPluginOps extends markdownit.Options {
    editorId: string;
}
declare const codetabs: (md: markdownit, _opts: CodeTabsPluginOps) => void;
export default codetabs;
